# Karma Agent API Keys
# Get these from https://agents.karmapay.xyz/api/register

# Owner key — full access (create cards, set limits, freeze, withdraw)
KARMA_API_KEY=sk_live_...

# Agent key — scoped to a single card (check balance, get card details, verify spend)
KARMA_AGENT_KEY=sk_agent_...
